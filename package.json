{
  "name": "nanban",
  "version": "1.0.0",
  "description": "Generate images with Google Gemini from your terminal",
  "bin": {
    "nanban": "dist/index.js"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun build src/index.ts --outfile dist/index.js --target node --external puppeteer --banner '#!/usr/bin/env node' && chmod +x dist/index.js",
    "prepublishOnly": "bun run typecheck && bun run build"
  },
  "dependencies": {
    "puppeteer": "^24.3.0"
  },
  "devDependencies": {
    "@clack/prompts": "^1.0.1",
    "@types/bun": "latest",
    "citty": "^0.2.1",
    "picocolors": "^1.1.1"
  }
}
